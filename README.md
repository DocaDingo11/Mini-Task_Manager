# Mini-Task_Manager
CPU and RAM checker
For running(make sure u have docker)
clone the repository
docker build -t system-monitor .
Running the container:
docker run -it --rm -e PYTHONUNBUFFERED=1 system-monitor
